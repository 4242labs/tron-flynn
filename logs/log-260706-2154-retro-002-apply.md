# Log — retro run-002 fixes applied + fleet-playbook deltas (PR #11)

Date: 2026-07-06 21:54. Maintenance session on the agent — applied the actionable
`retro/run-002-hiresling.md` findings plus 4 deltas from an external fleet-coordinator
playbook the operator supplied for comparison. Approved one at a time; merged as PR #11
(squash, `044f498`).

## Applied — retro findings
- **#1** `skill-pulse` / `install/tron-command`: session-id transform `sed 's/[^a-zA-Z0-9]/-/g'`
  (Claude Code strips ALL non-alnum, not just `/`) + non-empty guard treating an empty sidecar
  as a wall — an empty sid made the Stop-hook guard fire in every project session.
- **#2** `install/README`: settings merge committed BEFORE first background-job boot to kill the
  `bgIsolation:none` catch-22 at the root; `!`-shell only as fallback.
- **#3** `skill-manifest`: MANIFEST pinned to `<project-root>/.tron-flynn-manifest.md` (durable,
  sibling to the other flags) — not the job-local "scratchpad" that breaks resumability.
- **#4** `tron-flynn` Boot: `git fetch` + `pull --ff-only` before trusting any git-backed core doc.
- **#5** `tron-flynn` invariant: prefer `git -C`/absolute paths over stateful chained `cd`
  (proportionate discipline line; a PreToolUse hook judged overkill).
- **#7** `skill-dispatch`: challenge a "missing/off-limits" wall against docs + local state before
  relaying it to the operator.
- **#8** `tron-flynn` + `skill-manifest`: Architect persistence made mechanical — resumable Agent
  handle recorded in a new MANIFEST column, always resumed via `SendMessage`+handle, model re-pinned
  on resume. (Operator confirmed resume stays; playbook's never-resume rejected.)
- **#9/#10/#11** `skill-dispatch`: architect consults carry raw evidence + best-practice expectations,
  re-derive underlying facts (not just the reasoning chain), reject weaker alternatives with reason.
- **#12** `install/tron-command` Boot: write `<project-root>/CLAUDE.md` `@import` of the persona so
  invariants reload across compaction (Anthropic-sanctioned mechanism).
- **#13** `tron-flynn` + `skill-merge-close`: merge authority asked at boot; conditional delegation
  documented with minimum guardrails (CHALLENGE non-skippable, no auto-merge, bounded scope, stop-on-abnormality).
- **#14** `skill-merge-close`: post-merge — watch the merge commit's own CI + any triggered deploy to
  terminal, re-validate ALL ACs on trunk; no CI (docs) → confirm the merge landed clean.
- **#15** `skill-dispatch`: relaxing a worker's own stated boundary needs a FRESH self-contained
  dispatch — never a resumed relay, never TRON impersonating the operator, never TRON doing it itself.
- **#16** `skill-gates`: a worker's honest PARTIAL is a hard stop, never a "merge now anyway" option;
  TRON's status language mirrors the block file's literal field.
- **#17** `skill-operator-comms`: ANSWER/ACT/FLAG/FYI communication contract (operator-supplied),
  also saved to durable memory.
- **#18** `tron-flynn` invariant: least-privilege — commit/push/PR/merge are worker/operator actions;
  TRON is not granted those perms, a denial is a boundary signal not a missing allow-rule.
- **#19** `skill-gates`: a gate needing a live prod secret is SPLIT — operator runs the sensitive
  action, worker verifies the sanitized result; the secret never enters an agent transcript.
- **#21** `skill-manifest` + `skill-pulse`: rule out own fleet (full dispatch history) before blaming
  an external actor; worktree-discipline sweep for stray files in the shared checkout.
- **#20/#26** `skill-pulse`: active `SendMessage` ping every cycle once a PR is green / overnight —
  artifact polling can't distinguish idle from dead.
- **#23** `skill-operator-comms` + `install/README`: Telegram is outbound-only (send-only) today.

## Applied — fleet-playbook deltas (operator-approved 1–5)
- **Pin model** `skill-dispatch` + `tron-flynn`: model set explicitly on every spawn AND resume,
  never inherit the session default (silent downgrade).
- **#2** `skill-gates`: reviewer hunts green-but-wrong — shortcut-path tests + wrong-scope validation.
- **#3** `skill-dispatch`: workers emit a progress line at every real step.
- **#4** `skill-pulse`: signals can lie (mtime lag, self-refreshing heartbeats) — weight real
  commits/edits over mtimes and self-reported liveness.
- **#5** `tron-flynn`: concurrency cap counts reviewers/fixers, only the out-of-pool Architect excepted.

## Not applied
- Retro **#6** (workers not individually operator-visible) — accepted design, no action.
- Retro **#22** (Railway CLI global link state) — project-tooling concern, belongs in the project's docs.
- Retro **#25** (every PR its own click) — already covered by `skill-merge-close` + #18.
- Playbook **#6** (never kill on inference) — Flynn already pings before declaring dead; not adopted.
- Playbook "never resume, always spawn fresh" — rejected; Flynn keeps Architect resume (#8).

Not a fleet run — no run flags, MANIFEST, or PULSE to tear down; nothing to deactivate.

End of line.
