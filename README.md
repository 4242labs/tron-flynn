# tron-flynn

> ⚠️ **Experimental** — under active development; structure and interfaces may change without notice.

LLM-run TRON: a supervisor agent for Claude Code that runs a fleet of worker agents against a project's pipeline. Persona in `tron-flynn.md`, procedures in `skills/`, setup in `install/README.md`.
